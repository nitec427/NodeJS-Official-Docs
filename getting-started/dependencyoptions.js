// --save-dev devDependency

// --no-optional : will prevent optional dependencies from being listed

// --no-save install but do not save as project dependency

/* -S : --save -D: --save-dev  -O: --save-optional  */

// optionaldDepencies means that build failure of this dependency won't cause installation to fail. It is your responsibility to handle the lack of dependency
